缓存与多租户情况下的配合
读写请求成本差异
多租户下gc处理哪些块，gc触发时机
通道与时间耦合分配，调度时不止关注时间，还关注占用了几个通道。SSDKeeper这篇论文大概率导致租户间资源分配不均。
数据放置与多租户结合。
SLC，MLC的合理分配。
考虑到通道切换延迟。
多租户下的地址映射。
提出一种基于访问频率互补性的数据放置算法。该算法通过识别读热度与写热度在时间轴上的对应关系，将具有高读干扰风险的数据与高失效速率的数据放置在同一个块中，从而利用热写失效来抵消读干扰引发的搬迁开销