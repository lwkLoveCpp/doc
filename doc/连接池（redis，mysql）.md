这几个中间件的连接池本质上就是提前建立多个tcp连接，提高并发量。
（1）服务器端调用listen原语，监听端口
（2）客户端调用connect原语，主动建立连接。并对socket赋值上由系统分配的端口，用于后续向服务器发送数据。
（3）服务器调用accept原语，返回一个新的socket，代表建立好的tcp连接，通过该连接发送数据。
经过这几个步骤完成三次握手成功建立tcp连接。
连接池便是多个tcp连接，客户端通过多次调用connect语句与服务器建立多个tcp连接，这多个tcp连接的区别是客户端端口不一样。